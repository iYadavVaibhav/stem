# My Notes

```sh
# Change SSH Key to be used in Git Repo
git config --global core.sshcommand 'ssh -i /Users/vab/.ssh/id_rsa_vebs0205 -F /dev/null'
git config --global user.name "Vaibhav Yadav"
git config --global user.email "vebs0205@gmail.com"
```
